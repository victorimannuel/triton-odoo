<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="res_city_view_search" model="ir.ui.view">
        <field name="name">res.city.search</field>
        <field name="model">res.city</field>
        <field name="arch" type="xml">
            <search>
                <field name="name"/>
                <field name="code"/>
                <field name="state_id"/>
                <filter name="active" string="Active" domain="[('active', '=', True)]"/>
                <filter name="inactive" string="Inactive" domain="[('active', '=', False)]"/>
            </search>
        </field>
    </record>

    <record id="res_city_view_list" model="ir.ui.view">
        <field name="name">res.city.list</field>
        <field name="model">res.city</field>
        <field name="arch" type="xml">
            <list>
                <field name="code"/>
                <field name="name"/>
                <field name="state_id"/>
                <field name="active"/>
            </list>
        </field>
    </record>

    <record id="res_city_view_form" model="ir.ui.view">
        <field name="name">res.city.form</field>
        <field name="model">res.city</field>
        <field name="arch" type="xml">
            <form>
                <sheet>
                    <group>
                        <field name="name"/>
                        <field name="code"/>
                        <field name="state_id"/>
                        <field name="active"/>
                    </group>
                    <group>
                        <field name="district_ids"/>
                    </group>
                </sheet>
            </form>
        </field>
    </record>

    <record id="res_city_action" model="ir.actions.act_window">
        <field name="name">Kabupaten / Kota</field>
        <field name="res_model">res.city</field>
        <field name="view_mode">list,form</field>
    </record>

    <menuitem id="res_city_menu" parent="contacts.menu_localisation" action="res_city_action" sequence="91" groups="base.group_system"/>
</odoo>
