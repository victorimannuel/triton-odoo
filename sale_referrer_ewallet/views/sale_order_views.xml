<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="view_order_form_referrer" model="ir.ui.view">
        <field name="name">sale.order.form.referrer</field>
        <field name="model">sale.order</field>
        <field name="inherit_id" ref="sale.view_order_form"/>
        <field name="arch" type="xml">
            <xpath expr="//header" position="inside">
                 <button name="action_open_apply_credit_wizard" 
                        string="Apply Referrer Credit" 
                        type="object" 
                        class="btn-secondary"
                        invisible="state != 'draft' or not partner_id"/>
            </xpath>
            <xpath expr="//button[@name='action_confirm'][1]" position="attributes">
                <attribute name="confirm">Are you sure you want to confirm this order? The referrer (if any) will receive their commission upon confirmation.</attribute>
            </xpath>
            <xpath expr="//button[@name='action_confirm'][2]" position="attributes">
                <attribute name="confirm">Are you sure you want to confirm this order? The referrer (if any) will receive their commission upon confirmation.</attribute>
            </xpath>
            <xpath expr="//field[@name='partner_id']" position="after">
                <field name="referrer_partner_id" 
                       domain="[('id', '!=', partner_id)]"
                       context="{'default_is_company': True}"
                       placeholder="Select Referrer"/>
                <field name="referral_commission_percentage" invisible="not referrer_partner_id"/>
                <field name="referral_credit_amount" invisible="not referrer_partner_id"/>
            </xpath>
        </field>
    </record>
</odoo>
