<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="view_apply_referrer_credit_wizard_form" model="ir.ui.view">
        <field name="name">apply.referrer.credit.wizard.form</field>
        <field name="model">apply.referrer.credit.wizard</field>
        <field name="arch" type="xml">
            <form string="Apply Credit">
                <group>
                    <field name="current_credit_balance" widget="monetary" options="{'currency_field': 'currency_id'}"/>
                    <field name="max_redeemable_amount" widget="monetary" options="{'currency_field': 'currency_id'}" invisible="1"/>
                    <field name="amount_to_redeem" widget="monetary" options="{'currency_field': 'currency_id'}"/>
                </group>
                <footer>
                    <button string="Apply" name="action_apply_credit" type="object" class="btn-primary"/>
                    <button string="Cancel" class="btn-secondary" special="cancel"/>
                </footer>
            </form>
        </field>
    </record>

    <record id="action_apply_referrer_credit_wizard" model="ir.actions.act_window">
        <field name="name">Apply Credit</field>
        <field name="res_model">apply.referrer.credit.wizard</field>
        <field name="view_mode">form</field>
        <field name="target">new</field>
    </record>
</odoo>
